{
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "publish": "bash deploy.sh"
  },
  "devDependencies": {
    "vuepress": "^1.4.0"
  },
  "dependencies": {
    "travis": "^0.1.1"
  }
}
